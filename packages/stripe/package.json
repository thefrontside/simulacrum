{
  "name": "@simulacrum/stripe-simulator",
  "version": "0.0.1",
  "description": "Simulate Stripe API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b tsconfig.dist.json",
    "start": "PORT=4001 nodemon --exec 'npm run build && node bin/start.mjs'",
    "test": "NODE_ENV=test mocha -r ts-node/register --timeout 10000 test/**/*.test.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thefrontside/simulacrum.git"
  },
  "keywords": [
    "simulacrum",
    "simulation",
    "stripe"
  ],
  "author": "Frontside Engineering <engineering@frontside.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/thefrontside/simulacrum/issues"
  },
  "homepage": "https://github.com/thefrontside/simulacrum#readme",
  "dependencies": {
    "effection": "^2.0.1",
    "fastify": "^3.21.1",
    "@simulacrum/server": "^0.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.14",
    "@simulacrum/client": "^0.5.2"
  }
}
